dataset:
  key: null
  info:
    img_dir: /scratch/generalvision/ADE20k
    anno_dir: /scratch/generalvision/null

decoder:
  key: DenseType
  params:
    image_size: ${model.image_size}
    input_dim_list: ${backbone.hidden_dim}
    hidden_dim: ${model.hidden_dim}
    dropout: 0.1
    num_classes: 150

num_epochs: 100
loss: CrossEntropyLoss

metrics: EvalMask

visualize: VisMask
